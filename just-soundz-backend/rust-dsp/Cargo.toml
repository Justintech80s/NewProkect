[package]
name = "just-maker-dsp"
version = "0.1.0"
edition = "2021"

[lib]
name = "just_maker_dsp"
crate-type = ["cdylib"]

[features]
default = ["python-extension"]
python-extension = ["pyo3/extension-module"]

[dependencies]
pyo3 = { version = "0.22" }
