[package]
name = "just-maker-dsp"
version = "0.1.0"
edition = "2021"

[lib]
name = "just_maker_dsp"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
