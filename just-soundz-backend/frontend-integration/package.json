{
  "name": "just-maker-composition-integration",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test composition-client.test.js && tsx --test local-sequencer.test.ts && npm run typecheck",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
